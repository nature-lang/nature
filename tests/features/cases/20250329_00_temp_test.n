type ellipse_t = struct{
    int rx
    int ry
}

type shape = union{
    int circle
    int square
    (int, int) rectangle
    ellipse_t ellipse
    void point
}

fn main() {
    shape o = shape.sllipse(ellipse_t{rx:-12, ry:2325})
}