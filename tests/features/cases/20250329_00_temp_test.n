import fmt

fn breakfn() {
}

fn sprintf() {
    var sources = '%d'
    int idx = 0

    for idx < 2 { // i 的值被污染
        idx += 1

        breakfn()

        if sources[idx] == 0 {
            idx = idx + 1
        }

        u8 c = sources[idx]
    }
}

fn main():void! {
    fmt.printf('%d', 12)
}
