name = "package"
version = "1.0.0"
authors = ["Alice <a@example.com>", "Bob <b@example.com>"]
description = "Your package description"
license = "MIT"
type = "bin" # lib or bin

# these mock packages are maintained with the fixture, their root modules are exposed by `mod <package-name>`
[dependencies]
rand = { type = "local", version = "v1.0.1", path = "./mock/rand" }
syscall = { type = "local", version = "v1.0.1", path = "./mock/syscall" }
os = { type = "local", version = "v1.0.1", path = "./mock/os" }
local = { type = "local", version = "v1.0.0", path = "./mock/local" }
