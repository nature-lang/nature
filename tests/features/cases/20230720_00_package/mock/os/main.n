mod os

import syscall
import rand.other as rand_other
import rand.utils.pool
import rand.utils.seed

int count = 233

fn get_count():int {
    return count
}

fn dump() {
    rand_other.dump()
    pool.dump()
    seed.dump()
    syscall.dump()
    println("hello os")
}
