mod codec

import strings

// top-level globals are shared by the whole module
pub int shared_count = 7

pub fn decode(int v):int {
    return v + scale()
}

fn scale():int {
    return 2
}

fn decode_tag():string {
    return strings.join(['d', 'e', 'c'], '-')
}
