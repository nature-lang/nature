import fs
import syscall

fn main():void! {
    var f = fs.open('./test.txt', syscall.O_RDONLY, 0)

    // 从偏移量 12 开始读取到文件结尾
    var content3 = f.read_at(12, -1)
    println('read_at(12, -1): ', content3)

    f.close()
}