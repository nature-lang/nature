import std
import time

std.printf('hello %v, nice: %v, %%, float: %v\n', 'world', 12, 3.1415, 233)

var s = std.sprintf('err msg=%v, count=%v', 'this is failed', 253)
println(s, 'len=', s.len())

string str =  time.now().datetime()
std.printf('err msg=%v, %v, count=%v\n', 'time in', str,253)

std.printf('%0d\n', -12)
std.printf('%0d\n', 12)

std.printf('|%5d|\n', -12)
std.printf('|%5d|\n', 12)

std.printf('|%010d|\n', -12)
std.printf('|%010d|\n', 12)