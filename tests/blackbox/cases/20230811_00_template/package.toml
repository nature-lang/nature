name = "package"
version = "1.0.0"
authors = ["Alice <a@example.com>", "Bob <b@example.com>"]
description = "Your package description"
license = "MIT"
type = "bin" # lib or bin

templates = [
    'temps/zlib.temp.n',
]

[links]  # 支持 .o 或者 .a link 文件
#libz = { linux_amd64 = 'temps/libz.a' }

[dependencies]
