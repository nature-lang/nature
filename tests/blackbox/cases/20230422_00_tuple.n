var tup = (1, 'hello', true)

var first = tup[0]

tup[1] = 'nature'

print(first, tup[1], tup[2], '\n')

// tuple def
tup[1] = 'hello'
var (foo, bar, car) = tup
print(foo, bar, car, '\n')


// tuple destr
var a = 2
var b = 'world'
var c = false
(a, b, c) = tup
print(a, b, c, '\n')

var d = false
(foo, bar, car) = (2, 'world', d)
print(foo, bar, car, '\n')