fn hello() {
    println('hello nature')
    throw 'world error'
}

var err = try hello()
if err.has {
    print('catch err: ', err.msg, '\n')
}

fn one(bool need):int {
    if need {
        throw 'i am one error'
    }
    return 2
}

var (s, one_err) = try one(true)
if one_err.has {
    print('one(true) has err:', one_err.msg, '\n')
} else {
    print('one(true) not err, s=', s, '\n')
}

var (s1, one_err1) = try one(false)
if one_err1.has {
    print('one(false) has err:', one_err1.msg, '\n')
} else {
    print('one(false) not err, s1=', s1, '\n')
}