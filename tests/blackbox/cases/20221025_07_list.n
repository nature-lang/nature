[int] list = [1, 2]

list[1] = 22

var b = list[1] + 3

println(list[0], list[1], b)

// --- slice and concat
var l2 = ['a', 'b', 'c', 'd', 'e']
var l3 = l2.slice(1, 4)
for v in l3 {
    print(v)
}

println()

l2 = l2.concat(['f', 'g', 'h'])
for v in l2 {
    print(v)
}

