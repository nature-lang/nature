{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-nls",
            "type": "shell",
            "command": "cargo build && pnpm run build",
            "group": "build",
            "problemMatcher": [
                "$rustc"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}